# 旧消费者主体的固定测试输入；来源和改动边界由升级报告记录。

SOURCES = {
    "transaction": 'def _get_session_provider() -> SessionProvider | None:\n    """从当前应用上下文解析会话提供器。"""\n    app_context = ApplicationContext.current()\n    return app_context.get_bean(bean_type=SessionProvider)\n\ndef transactional(func: Callable) -> Callable:\n    """事务装饰器，自动开启事务上下文，支持嵌套"""\n\n    @functools.wraps(func)\n    async def wrapper(*args, **kwargs) -> Any:\n        """在事务上下文中执行被装饰的异步函数。"""\n        session_provider = _get_session_provider()\n        if not session_provider:\n            raise DatabaseSessionException(error_code=DatabaseErrorCodeConstants.DATABASE_SESSION_NOT_INITIALIZED)\n        async with session_provider.transactional_session():\n            return await func(*args, **kwargs)\n    return wrapper\n\ndef transactional_requires_new(func: Callable) -> Callable:\n    """事务装饰器：挂起外层事务并独立提交当前操作。"""\n\n    @functools.wraps(func)\n    async def wrapper(*args, **kwargs) -> Any:\n        session_provider = _get_session_provider()\n        if not session_provider:\n            raise DatabaseSessionException(error_code=DatabaseErrorCodeConstants.DATABASE_SESSION_NOT_INITIALIZED)\n        async with session_provider.requires_new_transactional_session():\n            return await func(*args, **kwargs)\n    return wrapper\n',
    "job": "async def orchestrated_job_entry(job_id: int, handler_name: str, handler_param: str | None) -> None:\n    \"\"\"APScheduler 调度的统一入口点\"\"\"\n    logger.debug('【JobEntry】接收调度任务: JobID={}, Handler={}', job_id, handler_name)\n    try:\n        orchestrator = get_bean(bean_type=JobOrchestratorService)\n        if not orchestrator:\n            raise JobException(error_code=JobErrorCodeConstants.SCHEDULER_NOT_INITIALIZED, msg='无法获取 JobOrchestratorService: JobID={}'.format(job_id))\n        await orchestrator.orchestrate(job_id=job_id, handler_name=handler_name, handler_param=handler_param)\n    except Exception as e:\n        logger.error('【JobEntry】任务编排异常: JobID={}, {} - {}', job_id, type(e).__name__, e)\n        raise\n",
    "ai_tool": 'class ListSmsTemplatesTool(SystemModuleTool):\n    """查询所有短信模板列表"""\n    name = \'list_sms_templates\'\n    description = \'查询系统中所有已配置的短信模板列表，返回模板名称、编码、内容和参数\'\n    category = \'sms\'\n    category_label = \'短信服务\'\n    permissions = [\'system:sms-template:query\']\n    permission_type = PermissionTypeEnum.LIST.code\n\n    async def execute(self) -> str:\n        """执行短信工具逻辑。"""\n        template_service: SmsTemplateService = get_bean(bean_type=SmsTemplateService)\n        templates = await template_service.get_sms_template_list()\n        vo_list = [SmsTemplateToolRespVO.model_validate(t) for t in templates]\n        return json.dumps([json.loads(vo.model_dump_json(by_alias=False)) for vo in vo_list], ensure_ascii=False)\n',
    "cache": 'class Cacheable:\n    _cache_handler_instance: CacheHandler | None = None\n\n    @classmethod\n    def _get_cache_handler(cls) -> CacheHandler:\n        """从 DI 容器懒加载缓存处理器。"""\n        if cls._cache_handler_instance is None:\n            from framework.starter_di.context.get_bean import get_bean\n            cls._cache_handler_instance = get_bean(bean_type=CacheHandler)\n        return cls._cache_handler_instance\n',
}
